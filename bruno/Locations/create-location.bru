meta {
  name: Create Location
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/locations
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "name": "City Moto Shop",
    "countryCode": "DE"
  }
}
