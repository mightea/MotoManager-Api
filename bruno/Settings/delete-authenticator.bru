meta {
  name: Delete Authenticator
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/settings/authenticators/{{authenticatorId}}
  body: none
  auth: none
}

headers {
  Cookie: mb_session={{sessionToken}}
}
