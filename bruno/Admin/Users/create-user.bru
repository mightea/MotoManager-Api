meta {
  name: Create User
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/admin/users
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "email": "user@example.com",
    "username": "newuser",
    "name": "New User",
    "password": "password",
    "role": "user"
  }
}
